# Reacharr Local Agent Configuration
# Copy this file to .env and customize the values

# Remote Server Configuration
REMOTE_SERVER_URL=https://reacharr.com
MQTT_BROKER_URL=mqtt://reacharr.com:1883
MQTT_PASSWORD=reacharr_agent_password

# Application Settings
NODE_ENV=production
LOG_LEVEL=info
CORS_ORIGIN=http://localhost:3001

# Development Settings (uncomment for local development)
# NODE_ENV=development
# LOG_LEVEL=debug
# MQTT_BROKER_URL=mqtt://localhost:1883
# MQTT_PASSWORD=development_password

# Firebase Configuration
# Copy your Firebase service account key to firebase-service-account.json
# Copy your Firebase web config to frontend/firebase-config.json

# Example Radarr/Sonarr Configuration (configured via web UI)
# These are set through the configuration interface, not environment variables
# RADARR_URL=http://your-radarr-server:7878
# RADARR_API_KEY=your-radarr-api-key
# SONARR_URL=http://your-sonarr-server:8989
# SONARR_API_KEY=your-sonarr-api-key

# Agent Configuration
NODE_ENV=production
PORT=3000
CONFIG_PATH=/app/data/config.json

# MQTT Configuration
MQTT_URL=mqtt://your-remote-mqtt-broker:1883
MQTT_USERNAME=your_mqtt_username
MQTT_PASSWORD=your_mqtt_password
MQTT_CLIENT_ID=reacharr-agent-{random}

# Firebase Configuration (for authentication)
FIREBASE_PROJECT_ID=managarr-7c808
FIREBASE_SERVICE_ACCOUNT_PATH=/app/firebase-service-account.json

# Radarr Configuration (will be overridden by config.json)
RADARR_URL=http://localhost:7878
RADARR_API_KEY=your_radarr_api_key

# Sonarr Configuration (will be overridden by config.json)
SONARR_URL=http://localhost:8989
SONARR_API_KEY=your_sonarr_api_key

# Logging
LOG_FILE=/app/data/agent.log

# Sync Configuration
SYNC_INTERVAL=30000
RETRY_ATTEMPTS=3
RETRY_DELAY=5000

# Security
API_KEY_HEADER=X-API-Key

# Health Check
HEALTH_CHECK_INTERVAL=60000 